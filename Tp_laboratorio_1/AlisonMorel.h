/** \brief suma 2 numeros con decimales y devuelve el resultado.
 *
 * \param operando 1 El primer numero a sumar con decimales.
 * \param operando 2 El segundo numero a sumar con decimales.
 * \return Un nuevo operando 3=operando1+operando2.
 *
 */

float suma(float operando1, float operando2);//primera funcion.Declaracion.
//TIPO NOMBRE FUNCION (TIPO_VARIABLE,....)

/** \brief resta 2 numeros con decimales y devuleve el resultado.
 *
 * \param operando 1 El primer numero a restar con decimales.
 * \param operando 2 El segundo numero a restar con decimales.
 * \return Un nuevo operando 3=operando1-operando2.
 *
 */

float resta(float operando1, float operando2);

/** \brief Divide dos numeros con decimales y devuleve el resultado.
 *
 * \param operando 1 El primer numero a dividir con decimales.
 * \param operando 2 El segundo numero a dividir con decimales.
 * \return Un nuevo operando 3=operando1/operando2.
 *
 */

float division(float operando1, float operando2);

/** \brief Multiplica dos numeros con decimales y devuleve el resultado.
 *
 * \param operando 1 El primer numero a multiplicar con decimales.
 * \param operando 2 El segundo numero a multiplicar con decimales.
 * \return Un nuevo operando 3=operando1*operando2.
 *
 */

float multiplicacion(float operando1, float operando2);

/** \brief Factorial del primer numero con decimales y devuelve el resultado.
 *
 * \param operando 1 El primer numero a sacar factorial con decimales
 * \return Su factorial=operando1*(operando1-1).
 *
 */

int sacarPrimerFactorial(int operando1);

/** \brief Factorial del segundo numero con decimales y devuelve el resultado.
 *
 * \param operando 2 El segundo numero a sacar factorial con decimales.
 * \return Su factorial=operando2*(operando2-1)
 *
 */

int sacarSegundoFactorial(int operando2);
